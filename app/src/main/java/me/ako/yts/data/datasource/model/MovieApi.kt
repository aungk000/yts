package me.ako.yts.data.datasource.model

import android.util.Log
import kotlin.reflect.full.memberProperties

object MovieApi {
    /*fun updateIfNull(movie: MovieEntity, block: () -> Unit) {
        Log.d("MovieApi", "--> START UPDATE MOVIE")
        for(i in MovieEntity::class.memberProperties) {
            if(i.get(movie) == null) {
                block()
                Log.d("MovieApi", "${i.name}: ${i.get(movie)}")
            }
        }
        Log.d("MovieApi", "--> END UPDATE MOVIE")
    }*/
}